import React from "react";
import Header from "./components/Header/Header";
function App() {
  return (
    <React.Fragment>
      <Header />
    </React.Fragment>
  );
}

export default App;

// HEADER: Includes NAV (Logo and menu), as well as a section HERO (H1 title, p tagline, btn-group (start for free, learn more)
// FIRST ARTICLE: Includes H2 section title, section img, two sections each with H3 titles
// SECOND SECTION: Includes section img, H2 section title, section info
// THIRD SECTION: Includes section img, two articles each with H3 titles and section info
// FOOTER: Includes Logo and footer elements
